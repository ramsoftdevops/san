pipeline {
    agent { label 'docker-maven-trivy' }

    stages {
        stage('Build') {
            steps {
                sh 'echo "hello"'
            }
        stage('trivy fs scan'){
         steps {
         sh 'trivy fs --exit-code 1 --severity HIGH,CRITICAL .'
           }
        }
    }
}
}

